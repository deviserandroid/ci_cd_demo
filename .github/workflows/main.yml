name: Build and Analyze

on:
  pull_request:
    branches:
      - main

  push:
    branches:
      - pipeline

jobs:
  build-android:
    runs-on: macos-latest

    steps:
      - name: Basic Setup via Composite
        uses: .github/actions/action_basic_setup.yml

      - name: Fetch plugins Flutter Project
        run: flutter pub get






